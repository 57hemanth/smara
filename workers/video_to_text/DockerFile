# Dockerfile
FROM node:20-alpine
RUN apk add --no-cache ffmpeg
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev
COPY . .
ENV NODE_ENV=production
# Cloudflare Container will route to this port (match Worker Container.defaultPort)
ENV PORT=8080
CMD ["node", "index.ts"]