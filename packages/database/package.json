{
  "name": "@smara/database",
  "version": "0.0.1",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply smara_db --local --config=wrangler.jsonc",
    "db:migrate:prod": "wrangler d1 migrations apply smara_db --config=wrangler.jsonc --remote",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251011.0",
    "@types/node": "^24.8.1",
    "drizzle-kit": "^0.29.1"
  }
}
